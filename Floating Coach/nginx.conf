server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: /dashboard, /login, etc. → index.html (client-side screens)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
